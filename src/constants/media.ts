/** Макс. сторона кадра при отправке (JPEG). */
export const MAX_SEND_DIM = 1080;

/** Интервал захвата и отправки видеокадров, мс. */
export const FRAME_MS = 100;

/** Размер буфера ScriptProcessorNode для захвата аудио. */
export const AUDIO_BUF_SIZE = 4096;

/** Желаемое разрешение видео с камеры (getUserMedia). */
export const VIDEO_IDEAL_WIDTH = 1920;
export const VIDEO_IDEAL_HEIGHT = 1080;
